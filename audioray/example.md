# Пример SummaryService

Реализовано в `src/summary/summary.service.ts`.

См. также:
- `POST /api/summary/generate` — audioray
- `GET /meetings/:id/summary` — backend
- вкладка «Выжимка» на странице встречи — frontend
